#include<iostream>
#include<deque>
using namespace std;

int main(){
    //insert ops: emplace_front(), emplace_rear(),emplace(), push_bacm(), push_front()
    //delete: pop_back(), pop_front(),clear(), erase()
    //access: at(), [], front(), back()
    //modifiers: resize(), swap();

    return 0;
}